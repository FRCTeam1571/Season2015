#include "OI.h"

#include "RobotMap.h"

OI::OI():
DriveRobotCommand(),
xbox(XboxPort)
{}

void OI::Poll() {
	//Check to see if we need to update the motor speed
	double dX = abs(xbox->GetX() - prevX);
	double dY = abs(xbox->GetY() - prevY);

}

Joystick* OI::GetController() { return xbox; }
